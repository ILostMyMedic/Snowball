import { translate } from "@vitalets/google-translate-api";

export default async function detectAndTranslate(
	text: string
): Promise<string> {
	// const result = await translate(text, { to: "en" });

	// return result.text;

	return text;
}
