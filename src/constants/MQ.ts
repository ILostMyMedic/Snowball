export enum QueueType {
	server = "server",
	client = "client",
}

const MessageQueue = {
	throw: "throw",
	hit: "hit",
	miss: "miss",
	duck: "duck",
	stand: "stand",
};

export default MessageQueue;
